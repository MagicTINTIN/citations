const { BaseInteraction, User } = require("discord.js");

module.exports = {
    /**
        *   
        *
        * @param {BaseInteraction} reaction reaction element
        * @param {User} user user who sent it
        */
    on: function (reaction, user) {

    }
}
